name : example

on : push
jobs :
    first_job : 
        runs-on : ubuntu-latest
        steps :
        - name : Welcome Message
          run  : echo "First Github actions job"
        - name : Checkout repo
          uses : actions/checkout@v4
        - name : list the files
          run  : | 
                 ls -lrth
                 cat README.md   
        # This is multi line command exe 

       # - name : Read the readme file contents
       #   run  : cat README.md
